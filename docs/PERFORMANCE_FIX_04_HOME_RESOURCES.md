# 第四轮低风险性能修复：首页重复资源引用清理

## 修复原因

第五轮只读定位发现首页存在重复 CSS / FontAwesome 引用，导致首页资源加载偏重。实时 API 请求已明显下降，因此本轮优先处理更低风险的首页重复资源引用问题。

## 本次处理

- 只清理首页模板中重复的 CSS / FontAwesome 引用。
- 保留必要的 style.css。
- 保留必要的 app.css。
- 保留必要的 app.js。
- 保留必要的 home-legacy.js。
- 不修改 CSS 文件内容。
- 不修改 JS 文件内容。
- 不修改业务逻辑。

## 本次没有修改

- 没有修改数据库
- 没有修改接口地址
- 没有修改接口参数
- 没有修改接口返回结构
- 没有修改权限
- 没有修改登录
- 没有修改开奖逻辑
- 没有修改 CSS 内容
- 没有修改 JS 内容
- 没有修改客服逻辑
- 没有修改会员逻辑
- 没有清缓存
- 没有清日志
- 没有重启服务
- 没有删除资源文件

## 验证方式

- 检查首页 style.css 出现次数。
- 检查首页 FontAwesome 出现次数。
- 检查 app.js 和 home-legacy.js 是否仍正常加载。
- 检查首页 HTTP 状态。
- 检查首页是否正常显示。
- 检查 Git diff 是否只涉及允许文件。
